// #region generated
// @generated-flag - Delete when editing
import { createArtwork } from '$scripts/renderer';
import '$scripts/globals';
const id = '18-unexpected-path-rule';
const day = '18';
const prompt =
	'Unexpected path. Draw a route that changes direction based on one very simple rule.';
// #endregion
// -------------------------------- DAY 18 --------------------------------
// Unexpected path. Draw a route that changes direction based on one very simple rule.

async function draw(
	_frameNumber: number,
	_frameT: number,
	_elapsed: number,
	_stop: () => void,
) {}

createArtwork(draw, {
	canvas,
	length: 60 * 6,
	videoName: id,
	save: false,
});
